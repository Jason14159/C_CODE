#include <stdio.h>

int sum(int n1, int n2) {

    return n1 + n2;
}

int main() {

    int result = sum(239884, 9293);
    printf("result = %d", result);
    printf("\nres= %d\n", result);
    return 0;
}